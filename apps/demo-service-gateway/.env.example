# Demo service port
PORT=11100

# This service is NOT Sigilum-aware.
# It only accepts requests that include the gateway-injected upstream secret.
DEMO_UPSTREAM_HEADER=X-Demo-Service-Gateway-Key

# Must match the local gateway connection id
DEMO_GATEWAY_CONNECTION_ID=demo-service-gateway

# Populate from:
#   .sigilum-workspace/gateway-connection-secret-demo-service-gateway
DEMO_UPSTREAM_KEY=replace_me
