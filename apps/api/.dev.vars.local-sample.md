# `.dev.vars` Local Sample

###################
# Local development profile with blockchain writes disabled.
# 
# Notes:
# - `BLOCKCHAIN_MODE=disabled` means blockchain jobs are skipped and not enqueued.
# - Set `RESEND_API_KEY` if you want webhook terminal-failure alerts enabled in local dev.
# - Keep this profile for fast local iteration when chain sync is not needed.
###################

ENVIRONMENT=development
ADAPTER_PROVIDER=cloudflare

JWT_SECRET=replace-with-local-dev-secret
JWT_EXPIRY=7d
ALLOWED_ORIGINS=http://127.0.0.1:5000,http://localhost:5000,http://localhost:8787

BLOCKCHAIN_MODE=disabled
BLOCKCHAIN_NETWORK=testnet
BLOCKCHAIN_RPC_URL=https://sepolia.base.org
SIGILUM_REGISTRY_ADDRESS=0xREPLACE_ME
RELAYER_PRIVATE_KEY=0xREPLACE_ME
MAX_PENDING_AUTHORIZATIONS=20
PENDING_AUTHORIZATION_EXPIRY_HOURS=24
CHALLENGE_EXPIRY_HOURS=1
DEBUG_LOGGING=true

WEBHOOK_RETRY_WINDOW_HOURS=24
WEBHOOK_FAILURE_THRESHOLD=10
WEBHOOK_ALERT_EMAIL_FROM=alerts@localhost
# RESEND_API_KEY=re_replace_local_key
# WEBHOOK_ALLOW_PRIVATE_TARGETS=true
# WEBHOOK_DNS_RESOLVER_URL=https://cloudflare-dns.com/dns-query
