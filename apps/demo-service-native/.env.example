# Demo service port
PORT=11000

# Demo Service (Native) uses Sigilum API mode only.
# Local default when running with scripts/run-local-api-gateway.sh.
SIGILUM_API_URL=http://127.0.0.1:8787
SIGILUM_API_KEY=replace_with_service_api_key_for_demo-service-native
