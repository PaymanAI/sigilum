{
  "name": "sigilum",
  "version": "0.1.0",
  "private": true,
  "description": "Identity for AI Agents — the consent layer for the agentic web",
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "config:guards": "./scripts/check-config-guards.sh",
    "api:build": "pnpm --filter @sigilum/api build",
    "api:dev": "pnpm --filter @sigilum/api dev",
    "api:typecheck": "pnpm --filter @sigilum/api typecheck",
    "api:test": "pnpm --filter @sigilum/api test",
    "api:clean": "pnpm --filter @sigilum/api clean && rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
